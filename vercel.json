{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm build",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD || echo 'build'",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
